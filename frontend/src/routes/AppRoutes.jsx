import { BrowserRouter } from 'react-router-dom';
import App from '../App.jsx';

export default function AppRoutes() {
  return (
    <BrowserRouter>
      <App />
    </BrowserRouter>
  );
}
